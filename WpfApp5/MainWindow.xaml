<Window x:Class="TaskManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TaskManager" Height="600" Width="1000" Background="#F5F5F7">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#2C3E50">
            <TextBlock Text="МЕНЮ" Foreground="White" Margin="20,30,0,10" FontWeight="Bold" FontSize="14"/>
            <Button Content="Все задачи" Background="Transparent" Foreground="White" BorderThickness="0" Height="40" HorizontalContentAlignment="Left" Padding="20,0,0,0"/>
            <Button Content="Сегодня" Background="Transparent" Foreground="White" BorderThickness="0" Height="40" HorizontalContentAlignment="Left" Padding="20,0,0,0"/>
            <Button Content="Выполнено" Background="Transparent" Foreground="White" BorderThickness="0" Height="40" HorizontalContentAlignment="Left" Padding="20,0,0,0"/>
        </StackPanel>
        
        
        
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,20">
                <TextBlock Text="Мои задачи" FontSize="28" FontWeight="Light" VerticalAlignment="Center"/>
                <Button Content="Обновить" Click="OnLoadTasksClick" DockPanel.Dock="Right" Width="100" Height="35" Background="#3498DB" Foreground="White" BorderThickness="0"/>
            </DockPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBox x:Name="NewTaskTitle" Width="400" Margin="0,0,10,0" Padding="5" VerticalContentAlignment="Center"/>
                <Button Content="+" Click="OnAddTaskClick" Width="40" Height="35" Background="#2ECC71" Foreground="White" FontSize="20" FontWeight="Bold" BorderThickness="0"/>
            </StackPanel>

            <DataGrid x:Name="TasksGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" 
                      Background="White" BorderThickness="0" GridLinesVisibility="None" RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                    <DataGridTemplateColumn Header="Название" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="150"/>
                    <DataGridTextColumn Header="Приоритет" Binding="{Binding Priority}" Width="100"/>
                    <DataGridTextColumn Header="Дедлайн" Binding="{Binding DeadlineDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>